{
  "name": "yandex-music-token",
  "version": "2.0.0",
  "private": false,
  "type": "module",
  "description": "Yandex Music token extractor and helper",
  "homepage": "https://ym-token.marshal.dev/",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "predeploy": "pnpm build",
    "deploy": "gh-pages -d dist -t true"
  },
  "devDependencies": {
    "gh-pages": "^6.2.0",
    "typescript": "^5.8.0",
    "vite": "^6.3.0"
  }
}
